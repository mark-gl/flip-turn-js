export type Point = {
  x: number;
  y: number;
};

export type Corner = "tl" | "tr" | "bl" | "br";

export type DisplayMode = "single" | "double";

export type DisplayOption = DisplayMode | "auto";

export type TurnDirection = "forward" | "backward";
